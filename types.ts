export type User = {
    name: string,
    id: string,
    email: string,
    password: string
}

export type State = {
    email:string,
    name: string,
    id: string
}

export type Video = {
    title: string,
    thumbnail: string,
    description: string,
    duration: string,
    youtubeid: string,
    date: string,
    id: string,
    fav:boolean
}